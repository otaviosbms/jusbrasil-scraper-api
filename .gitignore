node_modules/
dist/
.env
scripts/output-*.html

# Perfil persistente do Chromium (docs/AUTH.md) — depois de `npm run login` contém
# cookies de sessão do Jusbrasil, equivalente a uma senha. Nunca commitar, nunca compartilhar.
.jusbrasil-browser-profile/
*-browser-profile/
